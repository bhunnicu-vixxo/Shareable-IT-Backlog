# API URL — base URL for backend API calls
# Dev: http://localhost:1576/api (Vite proxies /api to backend port from VITE_PROXY_TARGET)
# Prod: In Docker deployment, nginx proxies /api to backend. Set to /api or leave as-is.
# Non-Docker prod: set to your backend URL, e.g. https://backlog.vixxo.internal/api
VITE_API_URL=http://localhost:1576/api

# Optional: Override Vite dev proxy target (default: http://localhost:3000)
# VITE_PROXY_TARGET=http://localhost:3000

# Optional: admin-only token for triggering sync and viewing detailed sync errors.
# When set, backend must also set SYNC_TRIGGER_TOKEN to the same value.
# VITE_SYNC_TRIGGER_TOKEN=your-admin-token-here
