# API URL — use same origin in dev so Vite proxies /api to backend (avoids CORS)
# Dev: http://localhost:1576/api (Vite proxies to backend port from VITE_PROXY_TARGET)
# Prod: set to your backend URL, e.g. https://api.example.com/api
VITE_API_URL=http://localhost:1576/api

# Show "Open in Linear" button (IT users with Linear access only). Omit or false for business users.
# VITE_SHOW_OPEN_IN_LINEAR=true

# Optional: admin-only token for triggering sync and viewing detailed sync errors.
# When set, backend must also set SYNC_TRIGGER_TOKEN to the same value.
# VITE_SYNC_TRIGGER_TOKEN=your-admin-token-here
