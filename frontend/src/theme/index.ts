/**
 * Theme barrel export — re-exports the Chakra system and raw color constants.
 *
 * Main theme configuration lives in `@/theme` (frontend/src/theme.ts).
 * This module provides the raw color constants for non-Chakra usage.
 */
export { system } from '../theme'
export { BRAND_COLORS } from './tokens'
