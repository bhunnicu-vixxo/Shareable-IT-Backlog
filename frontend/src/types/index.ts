/**
 * Shared type definitions for the Shareable Linear Backlog application.
 * Feature-specific types should be co-located in their feature directories.
 */

/** Placeholder — feature types will be added as stories are implemented. */
export type AppConfig = {
  apiUrl: string
}
