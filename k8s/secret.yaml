# This is a template. Actual secrets should be created via kubectl or a sealed-secrets controller.
# Do NOT commit real values to version control.
#
# Create the secret manually:
#   kubectl --context Validation-K8S -n bridge create secret generic slb-secrets \
#     --from-literal=DATABASE_URL='postgresql://tadmin:PASSWORD@transact-nonprod.postgres.database.azure.com:5432/linear?sslmode=require' \
#     --from-literal=DB_USER='slb_user' \
#     --from-literal=DB_PASSWORD='YOUR_DB_PASSWORD' \
#     --from-literal=LINEAR_API_KEY='lin_api_YOUR_KEY' \
#     --from-literal=SESSION_SECRET='YOUR_SESSION_SECRET' \
#     --from-literal=ALLOWED_ORIGINS='https://backlog.vixxo.internal'
#
apiVersion: v1
kind: Secret
metadata:
  name: slb-secrets
  namespace: bridge
  labels:
    app.kubernetes.io/part-of: shareable-linear-backlog
type: Opaque
stringData:
  DATABASE_URL: "postgresql://tadmin:CHANGE_ME@transact-nonprod.postgres.database.azure.com:5432/linear?sslmode=require"
  DB_USER: "tadmin"
  DB_PASSWORD: "CHANGE_ME"
  LINEAR_API_KEY: "lin_api_CHANGE_ME"
  SESSION_SECRET: "CHANGE_ME_GENERATE_WITH_openssl_rand_base64_48"
  ALLOWED_ORIGINS: "https://backlog.vixxo.internal"
